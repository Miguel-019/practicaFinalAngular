import { TextUpperPipe } from './text-upper.pipe';

describe('TextUpperPipe', () => {
  it('create an instance', () => {
    const pipe = new TextUpperPipe();
    expect(pipe).toBeTruthy();
  });
});
